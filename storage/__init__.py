"""Local storage layer: the shared knowledge store (共同数据库) foundation.

V0.9.6 adds this package. It archives scored evidence AFTER the report is
safely written, so runs are remembered across time. Nothing in this package
may change scoring, ranking, or report markdown output.
"""
